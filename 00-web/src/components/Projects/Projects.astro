---
import { PROJECTS } from "../../scripts/consts"
import "./projects.css"

const finalProjects = PROJECTS.filter((project) => !project.hidden).slice().reverse()
---

<div class="projects-grid ">
    {
    finalProjects.map((project, reversedIndex) => { 
    const index = finalProjects.length - reversedIndex - 1
    const num = `${index + 1}`.padStart(2, "0")

    return (
        <article class="project-card ">
            <a
                href={`/${project.slug}`}
                class="project-header "
            >
                <span class="">
                    #{num}
                </span>
                <img
                    class=""
                    src={`./projects/${project.image}.webp`}
                    alt={`Captura de pantalla del proyecto ${project.title}`}
                    onerror="this.onerror=null;this.src='./projects/js-new-project.webp';"
                />
            </a>
            <div class="project-info">
                <h2>
                    <a
                    href={`/${project.slug}`}
                    class=""
                    >
                    {project.title}
                    </a>
                </h2>
                <p class="">
                    {project.description}
                </p>
                <ul class="project-badges">
                    {project.learnings.map((learning) => (
                    <li>
                        {learning}
                    </li>
                    ))}
                </ul>
                <ul class="project-buttons">

                    <li>
                        <a
                            href={`https://github.com/ztevenx100/js_projects-collection/tree/main/${project.slug}`}
                            target="_blank"
                            rel="noopener noreferrer"
                        >
                            <svg class="icon icon-git">
                                <use xlink:href="/assets/img/svg/social-network.svg#svgGit"/>
                            </svg>
                            Código
                        </a>
                    </li>
                    <li>
                        <a
                            href={`/${project.slug}`}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="project-web"
                        >
                            <svg class="icon icon-web">
                                <use xlink:href="/assets/img/svg/icons.svg#svgWeb"/>
                            </svg>
                            {/* <svg class="icon">
                                <use xlink:href="/assets/svg/technologies.svg#svgJavascript"/>
                            </svg> */}
                            Demo
                        </a>
                    </li>
                    
                </ul>
            </div>
        </article>
      )
    })
    }

</div>